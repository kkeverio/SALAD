"""Model utilities and helpers used across SALAD.

This package previously stored only checkpoint files.  We add an
``__init__`` file so Python treats ``models`` as a package and allows us to
expose helper utilities such as the relation branch inference logic.
"""

__all__: list[str] = []

